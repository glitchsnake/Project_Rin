# Rin V9 — зависимости
# pip install -r req.txt

# Telegram
aiogram==3.*

# OpenAI-совместимый клиент (LM Studio)
openai>=1.0.0

# ── Категория 1-2: Векторная память ──────────────────────
chromadb>=0.4.0
sentence-transformers>=2.2.0

# ── Категория 3: Навыки ───────────────────────────────────
wikipedia>=1.4.0

# ── Категория 4: Голос (Whisper) ──────────────────────────
# Вариант A: через LM Studio (если поддерживает /v1/audio/transcriptions)
# — доп. установка не нужна, используется тот же openai-клиент

# Вариант B: локальный Whisper (раскомментировать если нужен офлайн-режим)
# openai-whisper>=20231117
# torch>=2.0.0

# ── Инфраструктура ────────────────────────────────────────
# SQLite встроен в Python — доп. установка не нужна

# ── Дополнительно V10 ─────────────────────────────────────
tiktoken>=0.7.0
aiosqlite>=0.20.0
python-dotenv>=1.0.0
